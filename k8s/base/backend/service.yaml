apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: habit-pet
  labels:
    app: backend
spec:
  selector:
    app: backend
  ports:
    - name: http
      port: 3000
      targetPort: 3000
  type: ClusterIP
